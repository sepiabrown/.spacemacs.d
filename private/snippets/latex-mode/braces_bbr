# -*- mode: snippet -*-
# name: braces_bbr
# regexp-key: \(.*\)bbr
# condition: (when (org-inside-LaTeX-fragment-p) 'auto)
# --
`(with-temp-buffer
(let ((str (nth 1 yas-matched-regexp-key-groups)))
(insert str)
(buffer-string)))`\left\\{ $1 \right\\} $0